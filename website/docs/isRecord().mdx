import Repl from '@/repl/Repl.tsx';
import CodeLink from '@/mdx-components/CodeLink.tsx';

# isRecord()

True if `maybeRecord` is a Record.

<Signature code={`isRecord(maybeRecord: unknown): boolean;`} />
