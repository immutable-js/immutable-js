import Repl from '@/repl/Repl.tsx';
import CodeLink from '@/mdx-components/CodeLink.tsx';

# isOrderedMap()

True if `maybeOrderedMap` is an OrderedMap.

<Signature code={`isOrderedMap(maybeOrderedMap: unknown): boolean;`} />
