import Repl from '@/repl/Repl.tsx';
import CodeLink from '@/mdx-components/CodeLink.tsx';

# isStack()

True if `maybeStack` is a Stack.

<Signature code={`isStack(maybeStack: unknown): boolean;`} />
